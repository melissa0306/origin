

<%= simple_form_for(@protocol) do |f| %>
<%= f.input :selected_means_of_transport, collection: @balance.means_of_transport, label: "Wähle Transportmittel", include_blank: false %>
<%= f.submit %>
<& end %> 

